Optional portable Node.js runtime folder.

If you want to use FF14 Collection Notebook on a PC without installing Node.js,
place a Windows Node.js executable here:

  runtime\node.exe

start-app.cmd checks this file before it looks for an installed Node.js.
